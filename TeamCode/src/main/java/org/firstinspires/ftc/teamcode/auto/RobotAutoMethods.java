package org.firstinspires.ftc.teamcode.auto;

public class RobotAutoMethods {
    /**
     * Moves the robot forward or backward with specified velocity.
     * @param velocity The velocity, in ticks per second.
     * @param forward If true, moves in the forward direction. Otherwise, moves in the reverse direction.
     */
    public static void drive(double velocity, boolean forward)
    {
        // TODO: define method drive() to move forward or backward with velocity
        // Your wheel motors can be accessed as follows:
        // RobotMap.rightFront
    }

    /**
     * Turns the robot left or right with specified velocity.
     * @param velocity The velocity, in ticks per second.
     * @param right If true, turns right. Otherwise, turns left.
     */
    public static void turn(double velocity, boolean right)
    {
        // TODO: define method turn() to turn left or right with velocity
        // Your wheel motors can be accessed as follows:
        // RobotMap.rightFront
    }

    /**
     * Strafes the robot left or right with specified velocity.
     * @param velocity The velocity, in ticks per second.
     * @param right If true, strafes right. Otherwise, strafes left.
     */
    public static void strafe(double velocity, boolean right)
    {
        // TODO: define method strafe() to strafe left or right with velocity
        // Your wheel motors can be accessed as follows:
        // RobotMap.rightFront
    }


    /**
     * Shoots three balls, using the belt, kick, and flywheel mechanism.
     * @param range The distance from the robot camera to the AprilTag, in inches
     */
    public static void shoot(double range)
    {

    }
}
